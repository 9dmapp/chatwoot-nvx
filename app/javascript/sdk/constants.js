export const BUBBLE_DESIGN = ['standard', 'expanded_bubble', 'box'];
export const WIDGET_DESIGN = ['standard', 'flat'];
export const DARK_MODE = ['light', 'auto', 'dark'];

// The widget answers in English unless a site deliberately asks for another language.
export const DEFAULT_LOCALE = 'en';

// The box launcher is a fixed vertical tab. Its label is part of the design rather than copy
// that follows the conversation or the visitor's language, so it never changes.
export const BOX_LAUNCHER_LABEL = 'Live Chat';
